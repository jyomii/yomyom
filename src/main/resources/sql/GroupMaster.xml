<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="GroupMaster">
	
	
	<insert id="insertGroup" parameterType="ggroup">
	insert into ggroup values(
	nvl((select max(groupkey) from ggroup)+1,0),
	 #{groupName},
	 #{userKey},
	 #{ageKey},
	 #{whereKey},
	 #{categoryKey},
	 #{groupInfo},
	 #{groupDFile },
	 #{groupIdOrigin},
	 #{groupCFile},
	 #{groupCOrigin},
	 #{groupPrivate},
	 #{groupType},
	sysdate,
	#{groupstatus}
	
	 )
	</insert>
	<select id="checkGroupName" parameterType="String" resultType="int">
		select count(*) from ggroup where groupName = #{name}
	</select>
</mapper>